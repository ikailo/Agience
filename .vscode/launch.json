{
    "version": "0.2.0",
    "configurations": [        
        {
            "name": "Identity",
            "type": "coreclr",
            "request": "launch",
            "preLaunchTask": "build-identity",
            "program": "${workspaceFolder}/core/authority/identity-api-dotnet/bin/Debug/net8.0/Agience.Authority.Identity.dll",
            "args": [],
            "cwd": "${workspaceFolder}/core/authority/",
            "stopAtEntry": false,
            "env": {
                "ASPNETCORE_ENVIRONMENT": "Debug",
                "ENV_FILE_PATH": "${workspaceFolder}/core/authority/.env",
                "WORKING_DIRECTORY": "identity-api-dotnet/"
            }
        }        
    ],
    "compounds": [
      {
        "name": "Launch All",
        "configurations": [          
          "Identity",
        ]
      }
    ]
}
